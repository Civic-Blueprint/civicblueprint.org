export function CoreClaim() {
  return (
    <section className="section-shell">
      <div className="container-shell">
        <p className="section-eyebrow mb-4">Core claim</p>
        <h2 className="section-title mb-5 text-ink">
          What if these failures share the same upstream causes?
        </h2>
        <p className="section-lead mb-6">
          The framework focuses on institutional capacity, democratic
          accountability, execution speed, and governance coordination failures
          to test whether those factors create recurring bottlenecks across very
          different domains.
        </p>
        <p className="max-w-3xl border-l-2 border-blueprint-line pl-4 text-[var(--step-0)] leading-relaxed text-muted">
          This is the shift from issue-by-issue commentary to upstream systems
          diagnosis.
        </p>
      </div>
    </section>
  );
}
